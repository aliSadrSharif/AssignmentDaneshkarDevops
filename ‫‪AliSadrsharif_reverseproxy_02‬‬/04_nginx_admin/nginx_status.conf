events {}
http {
    server {
        listen 80;
        location /basic_status {
            stub_status;
            allow all;
        }
        location / {
            return 200 "Nginx Status Page";
            add_header Content-Type text/plain;
        }
    }
}
