services:
  web:
    image: nginx:alpine
    ports:
      - "8088:80"
    volumes:
      - ./web:/usr/share/nginx/html:ro
    depends_on:
      - api

  api:
    image: alpine
    command: sh -c "while true; do sleep 30; done"
